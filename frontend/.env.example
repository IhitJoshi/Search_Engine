# Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend API URL (OPTIONAL)
# For local development with separate backend server:
# VITE_API_URL=http://localhost:5000

# For production (Vercel) or same-origin deployment:
# VITE_API_URL=
# (Leave empty to use relative paths)

# IMPORTANT:
# - No trailing slash
# - No quotes around the value
# - Must use VITE_ prefix for Vite to expose to client
# - When empty, API calls use relative paths (e.g., /api/stocks)
